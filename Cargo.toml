[package]
name = "odgi-ffi"
version = "1.0.0"
authors = ["Milan Langhendries founder@caelrith.com"]
edition = "2024"
description = "A safe, ergonomic Rust wrapper for the odgi pangenome graph tool."
license = "MIT"
repository = "https://github.com/caelrith/odgi-ffi"
readme = "README.md"
keywords = ["odgi", "pangenome", "graph", "ffi", "bioinformatics"]
categories = ["science", "api-bindings", "external-ffi-bindings"]

# This is the ONLY packaging key we need.
# It tells Cargo to package everything by default, EXCEPT for these
# large, non-essential files from the submodule.
exclude = [
    "vendor/odgi/.git",
    "vendor/odgi/.github/",
    "vendor/odgi/.gitignore",
    "vendor/odgi/docs/",
    "vendor/odgi/test/",
    "vendor/odgi/scripts/",
    "vendor/odgi/examples/",
    "vendor/odgi/Dockerfile",
]

[lib]
name = "odgi_ffi"
crate-type = ["rlib"]

[dependencies]
cxx = "1.0"
tempfile = "3.3.0"

[build-dependencies]
cxx-build = "1.0"
cmake = "0.1"
fs_extra = "1.3.0"

[dev-dependencies]
